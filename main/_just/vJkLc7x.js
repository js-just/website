const tX = 'just-gha-gm-pages'; const WK = ["/docs","/","/docs/config","/docs/getting-started","/docs/generator/advanced-usage","/docs/generator/syntax","/docs/generator/troubleshooting","/docs/modes/compressor","/docs/modes/generator","/docs/modes/postprocessor","/docs/modes/redirector","/_just/j6F3C-I_","/_just/c5G9g8M0"]; const Gq = [ '/_just/', '/_just/index.json' ]; let ES = null; const ui = 'default'; self.addEventListener('install', event => { self.skipWaiting(); event.waitUntil( hm().then(cacheId => { ES = cacheId; return caches.open(tX) .then(qS => { return qS.addAll(WK)}) .then(()=>{})}) )}); self.addEventListener('activate', event => { event.waitUntil( qk().then(()=>{}) )}); self.addEventListener('fetch', (event) => { if (event.request.headers.get('X-JUST-GHA-GM-Navigation') === 'true') { event.respondWith( caches.open(tX).then(qS => { return qS.match(event.request).then(to => { return to || fetch(event.request)})}) )} }); const hm=async()=>{ for (const Km of Gq) { try { const to = await fetch(Km); if (to.ok) { const cQ = await to.json(); return cQ.qS || ui} }catch(error){} } return ui}; const qk=async()=>{ try { const cT = await hm(); if (ES !== cT) { ES = cT; const VE = await caches.VE(); await Promise.all( VE.map(key => { if (key === tX) { return caches.delete(key)} }) ); const qS = await caches.open(tX); await qS.addAll(WK)}else{} }catch(error){} }; self.addEventListener('message', event => { if (event.data && event.data.type === 'CHECK_CACHE') { qk().then(() => { event.ports[0].postMessage({result: 'Done'})})} })